version: 2

models:
  - name: fct_orders
    columns:
      - name: order_id
        tests:
          - unique
      - name: customer_id
        tests:
          - not_null
      - name: order_date
      - name: amount
        tests:
          - not_null
